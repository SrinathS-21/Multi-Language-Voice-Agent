# Multilingual Voice Agent - Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         MULTILINGUAL VOICE AGENT SYSTEM                          │
│                                                                                  │
│  Supports dynamic language switching during calls based on user's spoken        │
│  language without pre-configuration or Convex database overrides                │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## Current Architecture (PROBLEM)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  CALL INITIALIZATION (Happens ONCE at start of call)                       │
└─────────────────────────────────────────────────────────────────────────────┘

                    ┌─────────────────┐
                    │  Incoming Call  │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │   Load Agent    │
                    │   from Convex   │
                    │   Database      │
                    └────────┬────────┘
                             │
              ┌──────────────▼──────────────┐
              │  agentConfig.language       │
              │  = 'en-IN' (FIXED)          │  ◄─── ❌ PROBLEM: Set once, never changes
              └──────────────┬──────────────┘
                             │
         ┌───────────────────┼───────────────────┐
         │                   │                   │
    ┌────▼────┐         ┌────▼────┐        ┌────▼────┐
    │   STT   │         │   TTS   │        │   LLM   │
    │ Plugin  │         │ Plugin  │        │ Plugin  │
    │────────│          │─────────│        │─────────│
    │ Lang:   │         │ Lang:   │        │ Lang:   │
    │ en-IN   │         │ en-IN   │        │ en-IN   │
    │ (FIXED) │         │ (FIXED) │        │ (FIXED) │
    └────┬────┘         └────┬────┘        └────┬────┘
         │                   │                   │
         └───────────────────┼───────────────────┘
                             │
                    ┌────────▼────────┐
                    │  Voice Session  │
                    │  (Immutable)    │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │  Call Proceeds  │
                    │  in English     │
                    │  (Cannot change)│
                    └─────────────────┘

❌ PROBLEM SUMMARY:
   • Language is set ONCE from Convex config
   • STT/TTS plugins created with FIXED language
   • No mechanism to detect/switch language mid-call
   • User speaks Tamil → Agent still responds in English
```

---

## New Architecture (SOLUTION)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  MULTILINGUAL DYNAMIC SWITCHING ARCHITECTURE                                │
└─────────────────────────────────────────────────────────────────────────────┘

INITIALIZATION (Setup Phase)
═══════════════════════════════════════════════════════════════════════════════

                    ┌─────────────────┐
                    │  Incoming Call  │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │   Load Agent    │
                    │   from Convex   │
                    └────────┬────────┘
                             │
              ┌──────────────▼──────────────────┐
              │  NEW Agent Config:              │
              │  • defaultLanguage: 'en-IN'     │  ◄─── ✅ Fallback only
              │  • supportedLanguages: [        │  ◄─── ✅ Can switch to any
              │      'en-IN', 'hi-IN', 'ta-IN'  │
              │    ]                             │
              │  • enableDynamicSwitching: true │  ◄─── ✅ Feature toggle
              └──────────────┬──────────────────┘
                             │
         ┌───────────────────┼───────────────────────────┐
         │                   │                           │
    ┌────▼─────────┐    ┌────▼─────────┐          ┌────▼─────┐
    │  STT Plugin  │    │  TTS Plugin  │          │  LLM     │
    │──────────────│    │──────────────│          │  Plugin  │
    │ Default:     │    │ Current:     │          │──────────│
    │ en-IN        │    │ en-IN        │          │ Context: │
    │              │    │              │          │ en-IN    │
    │ (Can detect  │    │ ✅ CAN       │          │          │
    │  multiple    │    │ SWITCH       │          │ ✅ CAN   │
    │  languages)  │    │ on-demand    │          │ UPDATE   │
    └──────────────┘    └──────────────┘          └──────────┘
                             │
                    ┌────────▼────────────┐
                    │  Voice Session      │
                    │  + Language Context │
                    └────────┬────────────┘
                             │
                    ┌────────▼────────────┐
                    │  NEW SERVICES:      │
                    │──────────────────── │
                    │  1. Language        │
                    │     Detector        │
                    │  2. Language        │
                    │     Switch Handler  │
                    └────────┬────────────┘
                             │
                    ┌────────▼────────────┐
                    │  Call Proceeds      │
                    │  (Dynamic Language) │
                    └─────────────────────┘

RUNTIME (Language Detection Loop)
═══════════════════════════════════════════════════════════════════════════════

   User Speaks                     Agent Listens
   ───────────────►               ──────────────────►
   
   "வணக்கம்"                      STT Transcribes
   (Tamil)                         ──────────────────►
                                   
                                   ┌─────────────────────────────┐
                                   │  Language Detector Service  │
                                   │─────────────────────────────│
                                   │  1. Analyze transcript      │
                                   │  2. Detect script:          │
                                   │     Tamil (U+0B80-U+0BFF)   │
                                   │  3. Confidence: 0.98        │
                                   │  4. Result: ta-IN           │
                                   └───────────┬─────────────────┘
                                               │
                                   ┌───────────▼─────────────────┐
                                   │  Hysteresis Buffer          │
                                   │  (Prevent False Switches)   │
                                   │─────────────────────────────│
                                   │  Detection #1: ta-IN        │
                                   │  Detection #2: ta-IN        │
                                   │  ✅ STABLE: Switch now!     │
                                   └───────────┬─────────────────┘
                                               │
                                   ┌───────────▼─────────────────┐
                                   │  Language Switch Handler    │
                                   │─────────────────────────────│
                                   │  Current: en-IN             │
                                   │  New: ta-IN                 │
                                   │  ⚡ TRIGGER SWITCH          │
                                   └───────────┬─────────────────┘
                                               │
                    ┌──────────────────────────┼──────────────────────────┐
                    │                          │                          │
           ┌────────▼────────┐        ┌────────▼────────┐      ┌─────────▼────────┐
           │  1. Switch TTS  │        │  2. Update LLM  │      │  3. Log Event    │
           │─────────────────│        │─────────────────│      │──────────────────│
           │ • Close en-IN   │        │ • Inject system │      │ • Session DB     │
           │   WebSocket     │        │   message:      │      │ • Analytics      │
           │ • Reconnect     │        │   "Respond in   │      │ • Metrics        │
           │   with ta-IN    │        │    Tamil"       │      │   Collector      │
           └────────┬────────┘        └────────┬────────┘      └─────────┬────────┘
                    │                          │                          │
                    └──────────────────────────┼──────────────────────────┘
                                               │
                                   ┌───────────▼─────────────────┐
                                   │  Agent Responds in Tamil    │
                                   │  "வணக்கம்! எப்படி இருக்கீங்க?" │
                                   └─────────────────────────────┘
                                               │
                                   ┌───────────▼─────────────────┐
                                   │  Conversation Continues     │
                                   │  in Tamil                   │
                                   │  (Until next language       │
                                   │   switch detected)          │
                                   └─────────────────────────────┘
```

---

## Component Interaction Diagram

```
┌────────────────────────────────────────────────────────────────────────────┐
│  COMPONENT INTERACTION FLOW                                                │
└────────────────────────────────────────────────────────────────────────────┘

┌──────────────┐     ┌──────────────────┐     ┌─────────────────────┐
│   LiveKit    │────▶│   Voice Agent    │────▶│   Agent Context     │
│   Room       │     │   Session        │     │─────────────────────│
└──────┬───────┘     └──────┬───────────┘     │ • sessionId         │
       │                    │                  │ • currentLanguage   │
       │ Audio Stream       │ Events           │ • languageDetector  │
       │                    │                  │ • switchHandler     │
       │                    │                  └─────────────────────┘
       │                    │
       │             ┌──────▼──────────┐
       │             │ Event Listener  │
       │             │ (User Transcript│
       │             │  Received)      │
       │             └──────┬──────────┘
       │                    │
       │                    │ transcript.text
       │                    │
       │             ┌──────▼──────────────────────────┐
       │             │  LanguageDetectorService        │
       │             │─────────────────────────────────│
       │             │  detectFromText(text)           │
       │             │    ↓                            │
       │             │  • scriptDetection()            │
       │             │    - Tamil: U+0B80-U+0BFF       │
       │             │    - Hindi: U+0900-U+097F       │
       │             │    - English: U+0000-U+007F     │
       │             │  • patternMatching()            │
       │             │  • confidenceScoring()          │
       │             │  • addToHistory()               │
       │             │    ↓                            │
       │             │  getStableLanguage()            │
       │             │    ↓                            │
       │             │  Returns: { language, conf. }   │
       │             └──────┬──────────────────────────┘
       │                    │
       │                    │ detection result
       │                    │
       │             ┌──────▼──────────────────────────┐
       │             │  LanguageSwitchHandler          │
       │             │─────────────────────────────────│
       │             │  handleTranscript()             │
       │             │    ↓                            │
       │             │  if (newLang != currentLang)    │
       │             │    ↓                            │
       │             │  performLanguageSwitch()        │
       │             └──────┬──────────────────────────┘
       │                    │
       │                    │ switch request
       │                    │
       │         ┌──────────┼──────────┐
       │         │          │          │
    ┌──▼─────────▼──┐  ┌───▼──────┐  ┌▼──────────────┐
    │  STT Plugin   │  │   TTS    │  │  LLM Plugin   │
    │───────────────│  │  Plugin  │  │───────────────│
    │ (Multi-lang   │  │──────────│  │ (Context      │
    │  detection)   │  │ switch   │  │  update)      │
    │               │  │ Language │  │               │
    │ Transcribes   │  │ (newLang)│  │ Inject system │
    │ audio to text │  │    ↓     │  │ message       │
    │               │  │ Close    │  │               │
    │               │  │ old WS   │  │               │
    │               │  │    ↓     │  │               │
    │               │  │ Reconnect│  │               │
    │               │  │ with new │  │               │
    │               │  │ language │  │               │
    └───────────────┘  └──────────┘  └───────────────┘
```

---

## Data Flow Diagram

```
┌────────────────────────────────────────────────────────────────────────────┐
│  DATA FLOW: USER UTTERANCE → LANGUAGE DETECTION → RESPONSE                │
└────────────────────────────────────────────────────────────────────────────┘

   USER                    STT                    DETECTOR              HANDLER
   ────                    ───                    ────────              ───────
     │                      │                         │                     │
     │  🗣️ "வணக்கம்"        │                         │                     │
     │  (Tamil audio)       │                         │                     │
     ├─────────────────────▶│                         │                     │
     │                      │                         │                     │
     │                      │  Transcribe audio       │                     │
     │                      │  to text                │                     │
     │                      ├────────────────────────▶│                     │
     │                      │  "வணக்கம்"              │                     │
     │                      │                         │                     │
     │                      │                         │  Detect language    │
     │                      │                         │  from script        │
     │                      │                         │  ↓                  │
     │                      │                         │  Tamil (U+0B80)     │
     │                      │                         │  detected           │
     │                      │                         │  ↓                  │
     │                      │                         │  Confidence: 0.98   │
     │                      │                         ├────────────────────▶│
     │                      │                         │  { lang: ta-IN }    │
     │                      │                         │                     │
     │                      │                         │                     │  Check current
     │                      │                         │                     │  language
     │                      │                         │                     │  ↓
     │                      │                         │                     │  en-IN → ta-IN
     │                      │                         │                     │  SWITCH NEEDED
     │                      │                         │                     │  ↓
     │                      │                         │         ┌───────────▼──────┐
     │                      │                         │         │  Switch TTS      │
     │                      │                         │         │  language to     │
     │                      │                         │         │  ta-IN           │
     │                      │                         │         └───────────┬──────┘
     │                      │                         │                     │
     │                      │                         │         ┌───────────▼──────┐
     │                      │                         │         │  Update LLM      │
     │                      │                         │         │  context         │
     │                      │                         │         └───────────┬──────┘
     │                      │                         │                     │
     │                      │                         │                     │
     │                  ┌───┴──────────────────────────────────────────────┴───┐
     │                  │  LLM generates response in Tamil                     │
     │                  │  "வணக்கம்! எப்படி இருக்கீங்க? எப்படி உதவ முடியும்?" │
     │                  └───┬──────────────────────────────────────────────────┘
     │                      │
     │                      │  TTS synthesizes in Tamil
     │                      │  ↓
     │  🔊 Tamil audio      │
     │◀─────────────────────┤
     │  response            │
     │                      │
```

---

## State Machine Diagram

```
┌────────────────────────────────────────────────────────────────────────────┐
│  LANGUAGE STATE MACHINE                                                    │
└────────────────────────────────────────────────────────────────────────────┘

                         ┌───────────────┐
                         │  CALL START   │
                         └───────┬───────┘
                                 │
                                 │ Load agent config
                                 │
                         ┌───────▼────────────────────┐
                         │  INITIAL STATE             │
                         │────────────────────────────│
                         │  currentLanguage: en-IN    │
                         │  (default from config)     │
                         │  detectionHistory: []      │
                         └───────┬────────────────────┘
                                 │
                                 │ First user utterance
                                 │
                         ┌───────▼────────────────────┐
                         │  DETECTING                 │
                         │────────────────────────────│
                         │  Analyze transcript        │
                         │  Detect language           │
                         │  Add to history            │
                         └───────┬────────────────────┘
                                 │
                     ┌───────────┴───────────┐
                     │                       │
          Detection = current       Detection ≠ current
          language (en-IN)          language (ta-IN)
                     │                       │
             ┌───────▼────────┐      ┌──────▼───────────────────┐
             │  STABLE        │      │  SWITCH PENDING          │
             │────────────────│      │──────────────────────────│
             │  Continue in   │      │  detectionHistory:       │
             │  en-IN         │      │  [ta-IN, ta-IN]          │
             │                │      │  Waiting for stability   │
             └───────┬────────┘      └──────┬───────────────────┘
                     │                      │
                     │ Next utterance       │ Next utterance
                     │                      │
                     └──────────┬───────────┘
                                │
                        ┌───────▼────────┐
                        │  Check         │
                        │  Hysteresis    │
                        └───────┬────────┘
                                │
                    ┌───────────┴──────────┐
                    │                      │
            < 2 consecutive       ≥ 2 consecutive
            detections            detections (STABLE)
                    │                      │
             ┌──────▼──────┐      ┌────────▼────────────────────┐
             │  WAIT       │      │  SWITCHING                  │
             │─────────────│      │─────────────────────────────│
             │  Keep       │      │  1. Close TTS connections   │
             │  current    │      │  2. Reconnect with new lang │
             │  language   │      │  3. Update LLM context      │
             └──────┬──────┘      │  4. Update currentLanguage  │
                    │             └────────┬────────────────────┘
                    │                      │
                    │                      │ Switch complete
                    │                      │
                    │             ┌────────▼────────────────────┐
                    │             │  STABLE (new language)      │
                    │             │─────────────────────────────│
                    │             │  currentLanguage: ta-IN     │
                    │             │  Agent responds in Tamil    │
                    │             └────────┬────────────────────┘
                    │                      │
                    └──────────────────────┘
                                 │
                                 │ Conversation continues...
                                 │
                         ┌───────▼───────┐
                         │  CALL END     │
                         └───────────────┘
```

---

## Database Schema Changes

```
┌────────────────────────────────────────────────────────────────────────────┐
│  CONVEX SCHEMA: agents TABLE                                              │
└────────────────────────────────────────────────────────────────────────────┘

BEFORE (Static Language):
═══════════════════════════════════════════════════════════════════════════════
┌─────────────────────────────────────────────────────────────────────────────┐
│  agents: defineTable({                                                      │
│    organizationId: v.string(),                                              │
│    name: v.string(),                                                        │
│    language: v.string(),  ◄─── ❌ PROBLEM: Single static language          │
│    voice: v.string(),                                                       │
│    systemPrompt: v.string(),                                                │
│    ...                                                                      │
│  })                                                                         │
└─────────────────────────────────────────────────────────────────────────────┘

AFTER (Dynamic Multi-Language):
═══════════════════════════════════════════════════════════════════════════════
┌─────────────────────────────────────────────────────────────────────────────┐
│  agents: defineTable({                                                      │
│    organizationId: v.string(),                                              │
│    name: v.string(),                                                        │
│                                                                             │
│    // ✅ NEW: Language configuration                                        │
│    defaultLanguage: v.optional(v.string()),      // 'en-IN' (fallback)     │
│    supportedLanguages: v.optional(                                          │
│      v.array(v.string())                         // ['en-IN', 'hi-IN', ...] │
│    ),                                                                       │
│    enableDynamicLanguageSwitching: v.optional(                              │
│      v.boolean()                                 // true/false              │
│    ),                                                                       │
│    languageSwitchSensitivity: v.optional(                                   │
│      v.union(                                    // 'high'|'medium'|'low'   │
│        v.literal('high'),                                                   │
│        v.literal('medium'),                                                 │
│        v.literal('low')                                                     │
│      )                                                                      │
│    ),                                                                       │
│                                                                             │
│    voice: v.string(),                                                       │
│    systemPrompt: v.string(),                                                │
│    ...                                                                      │
│  })                                                                         │
└─────────────────────────────────────────────────────────────────────────────┘

MIGRATION:
═══════════════════════════════════════════════════════════════════════════════
┌─────────────────────────────────────────────────────────────────────────────┐
│  // Convex migration script                                                 │
│  export default internalMutation({                                          │
│    handler: async (ctx) => {                                                │
│      const agents = await ctx.db.query('agents').collect();                │
│                                                                             │
│      for (const agent of agents) {                                          │
│        await ctx.db.patch(agent._id, {                                      │
│          defaultLanguage: agent.language,  // Copy old value                │
│          supportedLanguages: [agent.language],  // Single lang initially    │
│          enableDynamicLanguageSwitching: false,  // Off by default          │
│          languageSwitchSensitivity: 'medium',                               │
│        });                                                                  │
│      }                                                                      │
│    }                                                                        │
│  });                                                                        │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## File Structure

```
livekit_sarvam_agent/
│
├── src/
│   ├── services/
│   │   ├── agent-config.ts              (MODIFY - parse multi-lang config)
│   │   ├── language-detector.ts         (NEW - detect language from text)
│   │   └── language-switch-handler.ts   (NEW - orchestrate switching)
│   │
│   ├── plugins/
│   │   ├── sarvam_stt.ts               (MODIFY - multi-lang support)
│   │   ├── sarvam_tts.ts               (MODIFY - add switchLanguage())
│   │   └── factory.ts                   (MODIFY - create multi-lang plugins)
│   │
│   ├── agent/
│   │   └── index.ts                     (MODIFY - integrate language detection)
│   │
│   └── utils/
│       └── unicode-detection.ts         (NEW - script detection helpers)
│
├── convex/
│   ├── schema.ts                        (MODIFY - add multi-lang fields)
│   ├── agents.ts                        (MODIFY - update queries/mutations)
│   └── migrations/                      (NEW)
│       └── 001_add_multilang_support.ts
│
├── tests/
│   ├── language-detection.test.ts       (NEW - unit tests)
│   ├── language-switching.test.ts       (NEW - integration tests)
│   └── multilingual-e2e.test.ts        (NEW - end-to-end tests)
│
└── docs/
    ├── MULTILINGUAL_DYNAMIC_SWITCHING.md    (NEW - this document)
    ├── MULTILINGUAL_QUICK_START.md          (NEW - quick start guide)
    └── MULTILINGUAL_ARCHITECTURE.md         (NEW - architecture diagrams)
```

---

## Deployment Architecture

```
┌────────────────────────────────────────────────────────────────────────────┐
│  PRODUCTION DEPLOYMENT                                                     │
└────────────────────────────────────────────────────────────────────────────┘

                            ┌───────────────┐
                            │  Load Balancer│
                            └───────┬───────┘
                                    │
                    ┌───────────────┼───────────────┐
                    │               │               │
            ┌───────▼──────┐  ┌────▼─────┐  ┌──────▼──────┐
            │  Agent        │  │  Agent   │  │  Agent      │
            │  Worker 1     │  │  Worker 2│  │  Worker 3   │
            │───────────────│  │──────────│  │─────────────│
            │ Feature Flag: │  │ Feature  │  │ Feature     │
            │ DYNAMIC_LANG  │  │ Flag: ON │  │ Flag: ON    │
            │ = ON          │  │          │  │             │
            └───────┬───────┘  └────┬─────┘  └──────┬──────┘
                    │               │               │
                    └───────────────┼───────────────┘
                                    │
                    ┌───────────────▼───────────────┐
                    │  Convex Database              │
                    │───────────────────────────────│
                    │  • agents (multi-lang config) │
                    │  • callSessions               │
                    │  • callMetrics                │
                    │  • languageSwitchEvents       │
                    └───────────────┬───────────────┘
                                    │
                    ┌───────────────▼───────────────┐
                    │  Analytics & Monitoring       │
                    │───────────────────────────────│
                    │  • Language switch frequency  │
                    │  • Detection accuracy         │
                    │  • Switch latency             │
                    │  • False switch rate          │
                    └───────────────────────────────┘
```

---

**Document:** MULTILINGUAL_ARCHITECTURE.md  
**Version:** 1.0  
**Date:** January 26, 2026  
**Related Docs:**
- [Implementation Plan](./MULTILINGUAL_DYNAMIC_SWITCHING.md)
- [Quick Start Guide](./MULTILINGUAL_QUICK_START.md)
